import JsonIndex from "./component/indexJson";

const Json = () => {
  return (
    <div className="w-full min-h-full flex flex-col items-center py-16 bg-black/10">
      <JsonIndex />
    </div>
  );
};

export default Json;
